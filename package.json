{
  "name": "aam-helper",
  "displayName": "aam-helper",
  "description": "Support for AAML format",
  "repository": "https://github.com/ininids/aam-helper",
  "publisher": "ininids",
  "version": "0.0.3",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "aam",
        "aliases": [
          "AAM",
          "aam"
        ],
        "extensions": [
          ".aam"
        ],
        "configuration": "./language-configuration.json",
        "icon": {
          "light": "./icon.svg",
          "dark": "./icon.svg"
        }
      }
    ],
    "grammars": [
      {
        "language": "aam",
        "scopeName": "source.aam",
        "path": "./syntaxes/aam.tmLanguage.json"
      }
    ],
    "commands": [
      {
        "command": "aam-helper.helloWorld",
        "title": "Hello World"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.107.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.2",
    "ovsx": "^0.10.9",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0",
    "underscore": "^1.13.8"
  }
}
